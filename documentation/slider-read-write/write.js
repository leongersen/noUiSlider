// Set one value
$(".slider").val(10);
$(".slider").val([150]);

// Set the upper handle,
// don't change the lower one.
$(".slider").val([null, 14]);

// Set both slider handles
$(".slider").val([13.2, 15.7]);
